// Divider.js
const Divider = () => {
    return <hr style={styles.divider} />;
};

const styles = {
    divider: {
        height: '0.050rem',
        backgroundColor: '#ccc',
        marginBottom: '10px',
        width: '80%'
    },
};

export default Divider;
